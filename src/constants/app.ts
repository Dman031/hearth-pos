// The single source of truth for the user-facing app name. NEVER hardcode the
// brand string per screen — route every vendor-facing mention through APP_NAME.
// Currently "Deus"; a brand pass to "Flow" is planned pending name research, so
// centralizing here keeps that rename a one-line change.
export const APP_NAME = 'Deus';
